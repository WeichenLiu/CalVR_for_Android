#SET(ZLIB_SOURCE_DIR "${DEP_PROJ_DIR}/zlib")
#SET(ZLIB_BUILD_DIR "${ZLIB_SOURCE_DIR}/build/${ANDROID_ABI}")
#
#FILE(MAKE_DIRECTORY ${ZLIB_BUILD_DIR})
#ADD_SUBDIRECTORY(${ZLIB_SOURCE_DIR} ${ZLIB_BUILD_DIR})
#INCLUDE_DIRECTORIES(${ZLIB_SOURCE_DIR})
#INCLUDE_DIRECTORIES(${ZLIB_BUILD_DIR}/include)

SET(LIBJPEG_SOURCE_DIR "${DEP_PROJ_DIR}/libjpeg")
SET(LIBJPEG_BUILD_DIR "${LIBJPEG_SOURCE_DIR}/build/${ANDROID_ABI}")

FILE(MAKE_DIRECTORY ${LIBJPEG_BUILD_DIR})
ADD_SUBDIRECTORY(${LIBJPEG_SOURCE_DIR} ${LIBJPEG_BUILD_DIR})
INCLUDE_DIRECTORIES(${LIBJPEG_SOURCE_DIR})
INCLUDE_DIRECTORIES(${LIBJPEG_BUILD_DIR}/include)

SET(JPEG_LIBRARY "${LIBJPEG_BUILD_DIR}" PARENT_SCOPE)
SET(JPEG_INCLUDE_DIR "${DEP_PROJ_DIR}/include" "${LIBJPEG_SOURCE_DIR}" PARENT_SCOPE)
#message(STATUS "Jpeg found ${JPEG_INCLUDE_DIR}" )
SET(JPEG_FOUND "Yes" PARENT_SCOPE)
#find_package(JPEG REQUIRED) 


SET(LIBPNG_SOURCE_DIR "${DEP_PROJ_DIR}/libpng")
SET(LIBPNG_BUILD_DIR "${LIBPNG_SOURCE_DIR}/build/${ANDROID_ABI}")

FILE(MAKE_DIRECTORY ${LIBPNG_BUILD_DIR})
ADD_SUBDIRECTORY(${LIBPNG_SOURCE_DIR} ${LIBPNG_BUILD_DIR})
INCLUDE_DIRECTORIES(${LIBPNG_SOURCE_DIR})
INCLUDE_DIRECTORIES(${LIBPNG_BUILD_DIR}/include)

SET(PNG_LIBRARY "${LIBPNG_BUILD_DIR}" PARENT_SCOPE)
SET(PNG_INCLUDE_DIR "${DEP_PROJ_DIR}/include" "${LIBPNG_SOURCE_DIR}" PARENT_SCOPE)
SET(PNG_FOUND "Yes" PARENT_SCOPE)
#find_package(PNG REQUIRED) 

SET(LIBTIFF_SOURCE_DIR "${DEP_PROJ_DIR}/libtiff")
SET(LIBTIFF_BUILD_DIR "${LIBTIFF_SOURCE_DIR}/build/${ANDROID_ABI}")

FILE(MAKE_DIRECTORY ${LIBTIFF_BUILD_DIR})
ADD_SUBDIRECTORY(${LIBTIFF_SOURCE_DIR} ${LIBTIFF_BUILD_DIR})
INCLUDE_DIRECTORIES(${LIBTIFF_SOURCE_DIR})
INCLUDE_DIRECTORIES(${LIBTIFF_BUILD_DIR}/include)

SET(TIFF_LIBRARY "${LIBTIFF_BUILD_DIR}" PARENT_SCOPE)
SET(TIFF_INCLUDE_DIR "${DEP_PROJ_DIR}/include" "${LIBTIFF_SOURCE_DIR}" PARENT_SCOPE)
SET(TIFF_FOUND "Yes" PARENT_SCOPE)
#find_package(TIFF REQUIRED) 